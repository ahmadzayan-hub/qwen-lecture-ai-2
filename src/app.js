import "./styles.css"

// Bootstrap placeholder — full UI wiring lands in the UI task.
const app = document.getElementById("app")
app.innerHTML = '<div style="padding:40px;font-family:Cairo,sans-serif">Qwen Lecture AI — booting…</div>'
